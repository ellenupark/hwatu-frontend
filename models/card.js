class Card {
    constructor(category, image, matched, playerId, month) {
        this.category = category;
        this.image = image;
        this.matched = matched; 
        this.playerId = playerId;
        this.month = month;
    };
};

